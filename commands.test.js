const commands = require('./commands');

describe('the default command', () => {
  it('should error if there is no local metadata', () => {

  })

  it('should check for new entries if there is local metadata', () => {

  })

  it('should do an incremental update if there are new entries', () => {

  })

  describe('with a register argument', () => {
    it('should create the local metadata if missing', () => {

    })

    it('should do an incremental update if there are new entries', () => {

    })

    it('should not update other registers', () => {

    })
  })
});

describe('remove', () => {
  it('should remove the entry from registers.json', () => {

  })

  it('should remove the dataset', () => {

  })
})